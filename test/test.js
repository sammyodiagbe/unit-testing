import assert from "assert";

describe("Testing out function", () => {
  describe("return samson", () => {
    it("Should equals no name", () => {
      assert.equal("no name", "no name");
    });
  });
});
